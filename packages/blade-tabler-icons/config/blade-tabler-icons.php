<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default CSS class
    |--------------------------------------------------------------------------
    |
    | Applied to every <x-tabler-*> / <x-tabler-filled-*> component unless the
    | component itself is given a `class` attribute, which always wins.
    | Leave null to render icons with no class and size them via the SVG's
    | own width/height (24x24) or attributes you pass in manually.
    |
    */

    'class' => null,

    /*
    |--------------------------------------------------------------------------
    | Default attributes
    |--------------------------------------------------------------------------
    |
    | Extra HTML attributes merged onto the root <svg> of every icon, unless
    | overridden on the component itself. Useful for things like a default
    | aria-hidden="true" on decorative icons.
    |
    */

    'attributes' => [
        // 'aria-hidden' => 'true',
    ],

];
